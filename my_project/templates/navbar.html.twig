{% extends 'base.html.twig' %}
{% block navbar %}
    <h3>Navbar:</h3>
    <ul>
        <li><a href="{{ path('index') }}">index</a></li>
        {% if app.user %}
            <li><a href={{ path('logout') }}> logout</a></li>
            {% else %}
                <li><a href={{ path('login') }}> login</a></li>
        {% endif %}
    {% if app.user %}
        <li><a href="{{ path('user_index') }}">autoriai</a></li>
    {% endif %}
    {% if app.user %}
        <li><a href="{{ path('postsforadmin') }}">postai(admin view)</a></li>
    {% endif %}

    </ul>
{% endblock %}